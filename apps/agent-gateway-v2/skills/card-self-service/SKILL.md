# Card self-service

- Resolve the target card server-side; never let a driver select another driver’s card.
- Deliver a manual entry code only to the verified user’s private bot chat.
- Driver-name changes are owner-only even though unit number and driver ID may be self-service.
- Confirm every metadata change before executing it.
