---
description: When Verification tabs, gates, phases, routes, or write stores change, update the Verification skill in the same PR
globs: src/modules/verificationFlow/**/*,src/modules/verification/killSwitches.ts,src/routes/v1/verification*.ts,apps/mytrion-crm/src/mytrions/verification/**/*,apps/mytrion-crm/src/api/verificationFlow.ts,apps/mytrion-crm/src/api/verificationDeskWrites.ts,src/lib/mytrions.ts,apps/mytrion-crm/src/access/mytrions.config.ts
alwaysApply: false
---

# Verification skill stays in sync

You are on Verification paths. Product facts an agent cannot infer live in
`.claude/skills/verification-mytrion/SKILL.md` (mirrored to `.cursor/skills/`).

If this change touches **tabs, gates, phases, routes, or which store a write hits**, update
that skill in the **same PR** (both mirrors). Read the skill before editing Verification UI or APIs.

Do not invent CS / Billing workspace skills. Do not restyle `MytrionShell` sidebar/header.
Do not call credit_platform / loans from the CRM. The live desk is `/verification/flow/*`,
not the Sales verification tab and not the quarantined Decision Desk.
