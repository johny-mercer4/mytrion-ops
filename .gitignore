# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
*.tsbuildinfo

# Env & secrets
.env
.env.local
.env.*.local
backups/
!.env.example

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# Test / coverage
coverage/
.vitest/

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# Misc
tmp/
.cache/

# Generated metadata catalogs (keep the dir, ignore contents)
metadataScripts/output/*
!metadataScripts/output/.gitkeep
eval-reports/*
!eval-reports/.gitkeep
!eval-reports/baseline-*.json
.playwright-mcp/
/*.png
apps/design_handoff_octane_mini_app/

# Session scratch — not mergeable team state. Every agent used to append here, so every
# PR to `build` conflicted. Keep the file on disk; do not commit it.
WORKING_NOTES.md

# Local-only
.claude/settings.local.json
.DS_Store
_to_delete/

.agents/
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
